# Usage Instructions

1. **Run `get_valid_difference.sage`**
   This script generates valid input-output difference pairs and the corresponding weight `w` for each input difference.

2. **Run `running-step2-3-vars.sage`**
   This script searches for state values that satisfy the generated input-output difference pairs.

